<div class="rr-checkbox">
  <input id={{id}} type="checkbox" {{#if checked}}checked{{/if}}>
  <label for="{{id}}">
    <span>
      {{#if label }}
        {{t label}}
      {{/if}}
      {{#if @partial-block }}
        {{> @partial-block }}
      {{/if}}
    </span>
  </label>
</div>
