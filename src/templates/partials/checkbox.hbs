<div class="rr-checkbox">
  <input id={{id}} type="checkbox">
  <label for="{{id}}">
    <span>
      {{#if label }}
        {{t label}}
      {{/if}}
      {{#if @partial-block }}
        {{> @partial-block }}
      {{/if}}
    </span>
  </label>
</div>
